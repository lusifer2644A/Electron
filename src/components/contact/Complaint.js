import React from "react";

const About = () => {
    return (
        <div className="container">
            <div className="primary-color mainTitle">COMPLAINTS (BETA)</div>
        </div>
    );
};

export default About;
